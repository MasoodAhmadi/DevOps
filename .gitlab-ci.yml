stages:
  - build
  - test
  - deploy

# Global Docker image
image: docker:20.10.21

# Variables to configure Docker and Docker Compose
variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "" # Disable TLS

# Docker-in-Docker service configuration
services:
  - name: docker:20.10.21-dind
    alias: docker-dind # Unique alias for Docker-in-Docker
    command: ["--tls=false", "--host", "tcp://0.0.0.0:2375"]
build:
  stage: build
  script:
    - echo "Building Docker images..."
    - docker-compose -f docker-compose.yml build
    - docker images
    - docker-compose down

  tags:
    - ansible
test:
  stage: test
  services:
    - docker:dind
  script:
    - docker-compose --version
    - docker-compose ps
    - docker-compose -f docker-compose.yml up --scale test-service=0 -d # Start services in the background
    - docker-compose ps
    - docker-compose run -T test-service
    - docker-compose down
  only:
    - project
  tags:
    - ansible
#work at this moment 17.02.2025 at 4:35
#test works at this moment 20.02.2025 at 12:56
